name: Auth Telegram & Create Session

on:
  workflow_dispatch:
    inputs:
      phone_code:
        description: 'Код из Telegram (5-6 цифр)'
        required: false
        default: ''
        type: string

jobs:
  auth:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Telethon
        run: pip install telethon

      - name: Run auth
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          PHONE_NUMBER: ${{ secrets.PHONE_NUMBER }}
          PASSWORD: ${{ secrets.PASSWORD }}
          PHONE_CODE: ${{ github.event.inputs.phone_code }}
        run: python auth_local.py

      - name: Commit session
        if: success()
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          if [ -f "sessions/telegram_session.session" ] && [ -s "sessions/telegram_session.session" ]; then
            git add sessions/telegram_session.session
            git commit -m "Update telegram session $(date +'%Y-%m-%d %H:%M:%S')" || true
            git push || true
            echo "✅ Файл сессии обновлён!"
          else
            echo "⚠️ Файл сессии не создан"
          fi
